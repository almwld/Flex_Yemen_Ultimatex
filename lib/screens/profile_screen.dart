import 'package:flutter/material.dart';
class ProfileScreen extends StatelessWidget {
  const ProfileScreen({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text("حسابي وإعداداتي", style: TextStyle(color: Colors.white)));
}
