name: flex_yemen_ultimatex
description: "A professional Yemeni auction and marketplace platform."
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  
  # المكتبات المطلوبة التي أضفناها يدوياً
  supabase_flutter: ^2.0.2
  carousel_slider: ^4.2.1
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
